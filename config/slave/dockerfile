# 1
FROM jmeter-base

# Ports to be exposed from the container for JMeter Slaves/Server
EXPOSE 1099 50000

COPY entrypoint.sh /
COPY ../../keys/rmi_keystore.jks ${JMETER_BIN}/

# Application to run on starting the container
ENTRYPOINT ["/entrypoint.sh"]
